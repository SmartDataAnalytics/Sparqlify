package mapping;

import java.util.Arrays;

import org.aksw.commons.util.reflect.MultiMethod;

import com.hp.hpl.jena.sparql.expr.Expr;
import com.hp.hpl.jena.sparql.expr.ExprList;
import com.hp.hpl.jena.sparql.expr.aggregate.AggCount;
import com.hp.hpl.jena.sparql.expr.aggregate.Aggregator;

/*
public class AggregatorCopy {

	/**
	 * Creates a copy of an aggregator
	 * 
	 * 
	 * @param proto
	 * @param args
	 * /
	public Aggregator copy(Aggregator proto, Expr ...args)
	{
		ExprList list = new ExprList(Arrays.asList(args));
		return (Aggregator)MultiMethod.invoke(this, "_copy", proto, list);
	}
	
	
	public Aggregator copy(AggCount agg) {
		AggCount.
		//return new AggCount()
		return agg;
		
	}
		
}
*/
