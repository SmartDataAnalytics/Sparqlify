package domain;

/**
 * A relation definiton must provide the following information:
 * .) its columns
 * .) its access path capabilities
 *
 *
 * What I still don't understand is: How to compile a query expression into Query relation?
 * 
 * 
 * 
 * @author raven
 *
 */
public interface IRelation
{
	//IRelationSystem getRelationSystem();
}
